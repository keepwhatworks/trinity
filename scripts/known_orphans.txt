# Modules under src/trinity_local/ that are intentionally orphan from
# the production entry-point graph. Format: `path : reason`.
#
# Each line is a ratchet — when a new orphan appears (someone shipped
# code that's not reachable from main.py / mcp_server.py / the
# CORE_COMMAND_MODULES dispatch), CI fails until either:
#   1. The new module gets wired into a live caller, OR
#   2. It's added here with an explicit reason for why orphan is OK.
#
# Conversely, when an entry here stops being orphan (someone wired it
# up), CI also fails — remove the line so the whitelist reflects truth.
#
# See scripts/find_orphans.py for the analyzer.

src/trinity_local/me/correction_residual.py : The lens-as-seeker (basin_residuals / frontier) + axis_self_calibration. Measures the steer the four fixed taste axes can't name. Deliberately NOT wired into eval-prompt / council / dream per "prototype the meter, not the policy". Two 2026-06-02 retractions: (1) the first read "domain-local taste, no fifth axis" was wrong — axis_self_calibration shows the meter round-trips short text at residual 0.000; (2) my follow-up "register confound" was ALSO wrong (measured: short vs long prototypes give magnitude 0.945→0.954, coherence 0.105→0.106 — register adds ~nothing). The real flaw is the MEASURE + the grouping: residual_fraction divides by the content-dominated ‖pri−sac‖, and per-basin means reinforce topic (basin = topic cluster) — the one place content survives averaging. The VALIDATED path is content-invariant scoring: signed scalar shift c·a by sign-consistency recovers all 4 named axes at z=2.6–4.1; coherence-ranking (MRL of unit residuals) finds hidden structure where magnitude-ranking sees "all off-axis". basin_residuals/frontier (magnitude on per-basin means) are doubly confounded → stay orphaned. axis_self_calibration() is the in-register gate. Reachable via tests/test_correction_residual.py.

src/trinity_local/evidence_claims.py : The EVIDENCE half of the canonical-placeholder mechanism (2026-07-31). Orphan from the PRODUCTION entry-point graph by design and by direction: its consumers are the PUBLISHING boundary, not the runtime — `scripts/render_docs.py` (which imports it via sys.path, the same way it imports `facts.py`) and `tests/test_evidence_claim_guards.py`. It lives under src/ rather than in scripts/ because it must import the disagreement-ledger engine's own pre-registered floors (`MIN_TALLY_N`, `K4_MIN_RESOLVED`) rather than restate them — the whole point of the gate is that it reuses the engine's constants instead of inventing looser ones. Deliberately NOT called from `trust` / `ask` / the MCP surface: it recomputes numbers for DOCS, and wiring it into a runtime path just to satisfy this analyzer would be a fake caller. Reachable via `python scripts/render_docs.py` (step 5 of scripts/launch-check.sh) + 40 green tests.

src/trinity_local/__main__.py : Package entry point — invoked as `python3 -m trinity_local [--mcp]`, never imported, so it has no in-tree callers by design (same as any __main__.py). Added v1.7.306 as a robust fallback tier for the Claude Code plugin's MCP launcher (plugins/trinity-local/bin/trinity-mcp): when the `trinity-local` console script isn't on PATH but the package is importable, the launcher runs `python3 -m trinity_local --mcp`. Re-exports main.main(). Reachable via the plugin launcher + `python -m`; guarded by tests/test_plugin_manifest.py (asserts the launcher names this invocation).

# (baseline_floor.py entry removed 2026-07-11: run_floor_gate is now wired into
# handle_eval_run as the pre-report gate — the deferred #316 follow-up landed,
# so the module is reachable from commands/eval.py and no longer an orphan.)

# (pairwise_stats.py entry removed 2026-07-13: the Phase-0 falsifier
# returned FAIL with adequate power — module deleted per its own lifecycle;
# the experiment record lives in internal/experiments/pairwise-falsifier-2026-07-13/.)

src/trinity_local/me/exposures.py : The behavioral tier's exposure lane (council_9f4b3ab0a9b640c2, built 2026-07-14 post-K4-pass): honest per-family denominators — exposures (substantive provider-attributed at-bats from the prompt index) + ledger losses -> rejection rates with Wilson CIs. No approval inference (silence != evidence, council-banned). Live read: codex 0.039%, claude 0.157%, antigravity 0.334% per at-bat (routing-mix confound disclosed). Production wiring is GATED on K2 (founder kappa labels for the disagreement-ledger extractor) — when K2 clears, this surfaces on the launchpad/status as the Tier-1 readout and this line is removed; tests tests/test_exposures.py (4 green). Same annotated-orphan lifecycle baseline_floor/pairwise_stats had.

src/trinity_local/council_prosecution.py : Slice (c) pure core of the cross-provider PROSECUTION round (Gauntlet adversarial synthesis + Zhang decomposition on the cross-provider substrate). plan_prosecution routes each disputed council claim to the providers who did NOT make it (the cross-provider adversary — the moat); render_refutation_prompt is the small per-claim sub-call; prosecution_enabled gates the flag (TRINITY_PROSECUTE_ROUND, default OFF). Ships DORMANT by design: production wiring (run_prosecution_round + auto-fire in council_runner) is GATED on the forward-eval falsifier (internal/experiments/prosecution_round_forward_eval.py) — arm C must change arm B's verdict on >=20% of live councils, else the round stays off and the founder's "just prompt it better" null (slice b) wins. When the eval passes and the round is wired, this line is removed. Reachable via tests/test_council_prosecution.py (7 green) + the forward-eval harness. Same annotated-orphan lifecycle as exposures.py.

# (model_identity.py entry removed 2026-07-14: consolidate imports parse_identity
# for identity_weights — now production-reachable, no longer an orphan.)

# (disagreement_ledger.py entry removed 2026-07-18: wired into commands/trust.py
# (the `trust` verb in CORE_COMMAND_MODULES) — now production-reachable, no longer
# an orphan. The census-validated retrieval + the session-sampling resolver ship
# behind the trust CLI.)
src/trinity_local/skill_constitution.py : The frozen constitutional partition for the skill-revision loop (founder bent commitment #1 on 2026-08-10; fe8f4ff8/021d5d2a). Deliberately unwired: the loop's gate-1 consumer is the REVISION LOOP, which per the CoEvoSkills constraint (auto-revision without a verifier = 41.1% vs 53.5% human-curated, i.e. HARMFUL) must not run until a rule-quality verifier clears a floor — rule_witness (hq_065) is that instrument and has not cleared. Consumed today by tests/test_skill_loop.py (20 green, mutation-proven). When the verifier clears, the loop wires in and this line is removed. REVIEW NOTE 2026-08-11: violates_constitution guards the was→now direction only — it blocks removing/altering a locked line but NOT appending a contradiction ("the lock above is obsolete"); that hole is covered solely by gate 3 (human review of proposals) until closed.
src/trinity_local/skill_loop.py : The grounding loop (propose → gate → write a PROPOSAL, never a SKILL.md). Same lifecycle and same gate as skill_constitution.py above: production wiring is blocked on the rule-quality verifier (hq_065), because the council ratified that grounding alone is insufficient as an admission criterion (council_3448022415e7b7ea). Tests: tests/test_skill_loop.py. When the verifier clears, a CLI verb wires this in and this line is removed.
src/trinity_local/me/palate_canary.py : The collapse detector (P4, council_6ac26dafe733d16a). DELIBERATELY unwired: it exists so a detector is in place BEFORE any feedback edge is wired, rather than built afterwards and tuned on the data it must judge. Its halt bar (0.773, the pre-edge Wilson lower bound of 289/354) was frozen while no edge exists and nothing optimises against it. The consumer is the future loop, and the contract is that wiring ANY lens-feedback edge wires the consultation in the same commit — amd_0185's diode is an access-control property of that writer, not something this reader can verify. Tests: tests/test_palate_canary.py (pins that every state other than a demonstrated "alive" halts; mutation-proven three ways). Same lifecycle as skill_constitution.py above: this is the producer-without-a-consumer risk the council names, accepted knowingly because the alternative is a detector built after the thing it detects.
