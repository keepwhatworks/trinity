---
description: Route one question to the single best provider for it — cheap, one call, chairman-blessed.
argument-hint: <question>
---

Route this question to the best provider for it, then answer:

$ARGUMENTS

Call the `mcp__trinity-local__ask` tool from the connected Trinity MCP server (the
cheap 90% path — one provider, one call). If the argument is empty, ask the user
what they want routed first.
